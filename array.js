const numbers = [3,4]
// push method add new item at the end of the arry.
numbers.push(5,6);

// unshift method add new item beggining of the array.
numbers.unshift(6,7);

// splice method add middle of the array.
numbers.splice(1, 0, 'q', 'r');
console.log(numbers);

 String;
 Number;
 Boolean;
 null;
 undefined;
 


var array = ['a', 1, true, 2.3];
console.log(array);
